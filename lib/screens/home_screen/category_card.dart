import 'package:flutter/material.dart';

class categoryCard extends StatelessWidget {
  const categoryCard(int index, {Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.red,);
  }
}
